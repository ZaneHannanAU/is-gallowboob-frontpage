{
  "name": "is-gallowboob-frontpage",
  "version": "1.0.0",
  "description": "null",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm src/*.css src/*.js",
    "start": "live-server ./ --no-browser --port=9090 --ignore node_modules --ignore src",
    "make-windows": "make",
    "make": "./make"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "pug-cli": "^1.0.0-alpha6",
    "stylus": "^0.54.5"
  },
  "dependencies": {
    "stylus-material": "^1.4.3"
  }
}
