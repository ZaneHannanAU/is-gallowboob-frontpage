{
  "name": "is-gallowboob-frontpage",
  "version": "1.0.0",
  "description": "null",
  "license": "ISC",
  "main": "index.html",
  "scripts": {
    "test": "./make && npm start",
    "clean": "rm src/*.css src/*.js",
    "start": "live-server --ignore=node_modules --ignore=src --no-browser --port=9090 --wait=100 ../is-gallowboob-frontpage",
    "make-windows": "make",
    "make": "./make"
  },
  "dependencies": {
    "babel-runtime": "^6.22.0",
    "google-material-color": "^1.3.1",
    "stylus-material": "^1.4.3"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "pug-cli": "^1.0.0-alpha6",
    "stylus": "^0.54.5"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
